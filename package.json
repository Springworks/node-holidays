{
  "name": "node-holidays",
  "version": "0.1.0",
  "description": "Utils to manage public holidays.",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test istanbul cover --report teamcity --report lcov _mocha -- --ui bdd --check-leaks --slow 200 --reporter spec test",
    "lint": "gjslint -r . --strict --max_line_length 100 -e \".idea, coverage, node_modules\""
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Springworks/node-holidays.git"
  },
  "author": "Springworks",
  "bugs": {
    "url": "https://github.com/Springworks/node-holidays/issues"
  },
  "dependencies": {
    "moment": "^2.8.1"
  },
  "devDependencies": {
    "coveralls": "~2.10.0",
    "istanbul": "~0.2.11",
    "mocha": "~1.20.1",
    "mocha-lcov-reporter": "0.0.1",
    "should": "~4.0.4"
  },
  "license": "GPL v3"
}
